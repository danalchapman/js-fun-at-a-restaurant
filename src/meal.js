function nameMenuItem(name) {
  var menuItemName = name
  return `Delicious ${name}`
}


module.exports = {
  nameMenuItem,
  // createMenuItem,
  // addIngredients,
  // formatPrice,
  // decreasePrice,
  // createRecipe
}
